{
  "name": "@bkper/web-auth",
  "version": "1.0.0",
  "description": "Framework-agnostic OAuth authentication SDK for Bkper API",
  "author": "Bkper",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "LICENSE",
    "README.md"
  ],
  "scripts": {
    "clean": "rm -rf ./node_modules & rm -rf ./dist & wait",
    "prebuild": "bun install",
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "prepublishOnly": "bun run build && bun run test"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/bkper/bkper-web.git",
    "directory": "packages/auth"
  },
  "keywords": [
    "bkper",
    "auth",
    "oauth",
    "authentication",
    "sdk"
  ],
  "dependencies": {
    "js-cookie": "^3.0.5"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^4.0.16",
    "@vitest/ui": "^4.0.16",
    "@types/js-cookie": "^3.0.6"
  }
}
